id = "refactor-rename"
prompt = "Rename the shell function `calc_sum` to `compute_total` everywhere in this project (definition and all call sites). Behavior must stay identical; do not modify tests/check.sh."
fixture = "fixtures/refactor-rename"
timeout_secs = 300

[check]
cmd = "bash tests/check.sh"
