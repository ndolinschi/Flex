name: Nightly hardening

# Phase 3.2 / 3.4 skeletons + optional slow Tauri smoke (commented).
# PR-gate Playwright lives in ci.yml (desktop-e2e). This workflow is the
# slower / credential-gated layer and must not block merges.

on:
  schedule:
    # 05:17 UTC daily
    - cron: "17 5 * * *"
  workflow_dispatch:
    inputs:
      soak_turns:
        description: "browserMock soak turn count"
        required: false
        default: "5"

concurrency:
  group: nightly-hardening
  cancel-in-progress: true

jobs:
  # ── 3.2 lightweight soak (vite + browserMock, N turns) ────────────────────
  soak-browser-mock:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: packages/desktop
    env:
      CI: true
      SOAK_TURNS: ${{ github.event.inputs.soak_turns || '5' }}
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
          cache-dependency-path: packages/desktop/pnpm-lock.yaml
      - run: pnpm install --frozen-lockfile
      - run: pnpm exec playwright install --with-deps chromium
      # Keep vite in the same step as soak — background jobs die when a step ends.
      - name: Run soak against Vite preview
        run: |
          pnpm exec vite --host 127.0.0.1 --port 1420 --strictPort &
          vite_pid=$!
          trap 'kill $vite_pid 2>/dev/null || true' EXIT
          for i in $(seq 1 60); do
            if curl -fsS http://127.0.0.1:1420/ >/dev/null; then
              break
            fi
            if ! kill -0 "$vite_pid" 2>/dev/null; then
              echo "vite exited early" >&2
              exit 1
            fi
            sleep 1
            if [[ "$i" -eq 60 ]]; then
              echo "vite did not become ready" >&2
              exit 1
            fi
          done
          SOAK_BASE_URL=http://127.0.0.1:1420 SOAK_OUT=.soak/ci-summary.json pnpm soak
        env:
          SOAK_TURNS: ${{ github.event.inputs.soak_turns || '5' }}
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: soak-summary
          path: packages/desktop/.soak/
          if-no-files-found: ignore

  # ── 3.4 provider matrix (env-gated; soft-pass with no keys) ───────────────
  provider-matrix:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: packages/sdk
    # Repository secrets (optional). Missing keys → skip that provider.
    env:
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
      DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
      OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
      GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
      MISTRAL_API_KEY: ${{ secrets.MISTRAL_API_KEY }}
      XAI_API_KEY: ${{ secrets.XAI_API_KEY }}
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: |
            packages/sdk
            packages/providers
            packages/search
            packages/engine
      - name: Build flex binary
        run: cargo build -p agentloop-sdk --release --all-features
      - name: Provider matrix smoke
        run: |
          chmod +x scripts/provider-matrix-smoke.sh
          FLEX_BIN="$PWD/target/release/flex" ./scripts/provider-matrix-smoke.sh

  # ── 3.1 slow layer: real-window osascript smoke (DISABLED) ────────────────
  # Uncomment when a signed/unsigned .app artifact is available on the runner
  # and an Apple Silicon macOS job can launch the bundle headlessly enough for
  # Accessibility scripting. Keep this off the PR gate forever.
  #
  # osascript-smoke:
  #   if: false
  #   runs-on: macos-14
  #   steps:
  #     - uses: actions/checkout@v4
  #     - name: Build .app (unsigned)
  #       working-directory: packages/desktop
  #       run: |
  #         # pnpm install + pnpm tauri build --bundles app
  #         true
  #     - name: osascript smoke (stub)
  #       run: |
  #         # Example shape — replace with real Accessibility selectors once
  #         # the app exposes stable AX titles:
  #         #   open packages/desktop/src-tauri/target/release/bundle/macos/Flex.app
  #         #   osascript -e 'tell application "System Events" to …'
  #         echo "osascript smoke not wired yet"
  #         exit 0
